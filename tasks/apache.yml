#
#
#
---
- yum: pkg=${item} state=installed
  with_items:
  - mod_ssl
  tags:
  - httpd.pkgs

- copy: dest=/etc/httpd state=preset
  with_fileglob:
  - ${playbook_files}/${playbook}/*
  notify:
  - restart httpd
  tags:
  - httpd.cfg
