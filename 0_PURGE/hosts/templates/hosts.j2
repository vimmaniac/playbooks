# {{ ansible_managed }}
127.0.0.1       localhost.localdomain localhost
::1             localhost6.localdomain6 localhost6
#
{% for k,v in hostvars.iteritems() -%}
  {{ v['ansible_eth0']['ipv4']['address']}} {{ k }} {{ v['ansible_hostname'] }}
{% endfor %}
