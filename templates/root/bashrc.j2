# {{ ansible_managed }}

# Source global definitions
if [ -f /etc/bashrc ]; then
        . /etc/bashrc
fi

# User specific aliases and functions
alias vi=$(which vim || vi)

# Hack for python on CentOS5
export PATH=/usr/local/bin:$PATH
